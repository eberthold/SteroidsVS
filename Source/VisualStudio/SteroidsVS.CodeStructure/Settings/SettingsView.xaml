<UserControl x:Class="SteroidsVS.CodeStructure.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel>
            <GroupBox Header="Width Handling">
                <StackPanel Margin="12, 8">
                    <RadioButton>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Restore on file open to " />
                            <TextBox MinWidth="75" Text="{Binding DefaultRestoreWidth, Mode=TwoWay}" />
                            <TextBlock Text=" pixel (50 is min value)" />
                        </StackPanel>
                    </RadioButton>
                    <RadioButton Content="Restore last width from file" Margin="0, 8"/>
                    <RadioButton Content="Sync all code structure views" />
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
